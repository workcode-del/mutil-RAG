name: paper-rag
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip>=24
  - setuptools>=69
  - wheel

# 创建环境后，在仓库根目录执行：
#   conda activate paper-rag
#   pip install -e ".[unified]"
# CUDA版PyTorch应按部署机器的CUDA版本先安装到这个环境中。
