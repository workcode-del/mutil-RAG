# 主部署入口：所有模块安装到同一个 Python 3.11 虚拟环境。
# CUDA机器如需指定PyTorch CUDA wheel，请先按PyTorch官网命令安装torch，
# 再执行 `pip install -e ".[unified]"`；pip会复用已经安装的兼容版本。
-e .[unified]

# Qwen3-VL Embedding/Reranker的官方仓库不复制进本项目。
# 克隆到 third_party/Qwen3-VL-Embedding 后，在同一环境执行：
# pip install -e third_party/Qwen3-VL-Embedding
