# 可选的重排器最小依赖。主部署请使用 requirements/all.txt 单环境安装。
# Embedding和Reranker默认在同一进程；HTTP拆分仅是低显存备选模式。
transformers>=4.57.3,<6
accelerate>=1.12,<2
qwen-vl-utils>=0.0.14
numpy>=1.26,<3
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
